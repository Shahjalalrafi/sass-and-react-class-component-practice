import React from 'react';
import styles from './Home.module.css'

const Home = () => {
    return (
        <div>
            <h2 className={styles.title}>this is Home page</h2>
        </div>
    );
};

export default Home;