import React from 'react';
import styles from './Header.module.css'

const Header = () => {
    return (
        <div>
            <h4 className={styles.title}>this is header</h4>
        </div>
    );
};

export default Header;