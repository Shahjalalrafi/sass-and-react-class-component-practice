import React from 'react';
import style from './Footer.module.css'

const Footer = () => {
    return (
        <div>
            <p className={style.title}><small>this is footer</small></p>
        </div>
    );
};

export default Footer;